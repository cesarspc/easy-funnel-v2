# COD Commerce Platform Backend Dependencies
# Pinned versions for reproducible builds

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.28.1
# Multipart form parsing for banner image uploads (FastAPI UploadFile/Form)
python-multipart==0.0.20

# Database
prisma==0.15.0
psycopg[binary]==3.1.18

# Authentication
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
PyJWT==2.13.0

# Image Processing
Pillow==10.2.0

# Object Storage
boto3==1.34.34
botocore==1.34.34

# Redis
upstash-redis==1.7.0

# Validation and Serialization
pydantic==2.5.3
pydantic-settings==2.2.1

# Testing
# Kept compatible with requirements-dev.txt (pytest-asyncio>=0.24.0): the older
# 0.23.x pin cannot be installed alongside it, and pytest-asyncio 0.23.x fails
# to collect under pytest 8/9 (`Package` object has no attribute `obj`).
pytest==9.1.1
pytest-asyncio==1.4.0
hypothesis==6.98.2

# Linting and Type Checking
ruff==0.2.1
mypy==1.8.0

# Utilities
python-dotenv==1.0.0
