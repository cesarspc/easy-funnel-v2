# Public build-time configuration only. Never put secrets in VITE_* variables.
#
# Local `pnpm dev` can omit this and uses /api through Vite's localhost proxy.
# Production builds require an absolute HTTPS API root and fail if it is absent.
VITE_API_BASE_URL=https://api.example.com/api
