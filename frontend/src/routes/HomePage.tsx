/**
 * Placeholder root route. Replaced by the real storefront/admin entry
 * points as those features land (tasks 20-21).
 */
export function HomePage() {
  return (
    <main>
      <h1>COD Commerce Platform</h1>
      <p>SPA shell booted successfully.</p>
    </main>
  );
}
