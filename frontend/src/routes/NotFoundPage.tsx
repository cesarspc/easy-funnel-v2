/**
 * Generic not-found view. Also used for unknown/draft/paused/retired
 * landing slugs (Requirement 3.24) once the public landing route is wired.
 */
export function NotFoundPage() {
  return (
    <main>
      <h1>Página no encontrada</h1>
    </main>
  );
}
